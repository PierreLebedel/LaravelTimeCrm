# TimeCRM

Dans les diférents formulaires, le choix du projet est requis si le client dispose de projets.
D'ailleur si le client n'a qu'un seul projet, il faut le sélectionner par défaut.
Dans le cas inverse s'il n'y a pas de projet, le champs Projet apparait grisé (disabled).

Concernant Fullcalendar, j'ai fait quelques ajustement mais on va garder cette version. On peut supprimer la page de calendrier initiale.

Met à jour la doc avec ces nouveautés stp.